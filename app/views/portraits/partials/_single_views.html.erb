<%# portraits/partials/_single_views.html.erb %>

<div class="page-content">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-lg-8 col-lg-offset-2">
        <%# Title %>
        <h1><%= params[0] %></h1>
        <%# Teaser %>
        <h2 class="resume">
          <%= params[1] %>
        </h2>
      </div>
    </div>
    <%# Picture %>
    <div class="row contenu illustration">
      <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-lg-8 col-lg-offset-2">
        <%= params[2] %>
      </div>
    </div>
    <%# Body %>
    <div class="row contenu-principal">
      <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-lg-8 col-lg-offset-2">
        <%= params[3] %>
      </div>
    </div>

    <%# if the partial is called from the edit (or new) action,
      its params array has more than four items ()
    %>

    <% if params.size > 4 %>

    <!-- FIXME - put this into the css -->
    <style>
      div.my-inline-status-radios {
        margin-top: 32px;
      }
      div.my-inline-status-radios > div > div > span {
        display: inline-block;
        margin-left: 10px;
        margin-right: 10px;
      }
      div.my-inline-status-radios > div > div > label {
        display: block;
      }
      .my-submit-button {

      }
    </style>

    <%# status selection %>
    <div class="row my-inline-status-radios">
      <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-lg-8 col-lg-offset-2">
        <%= params[4] %>
      </div>
    </div>

    <%# submit button %>
    <div class="row">
      <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-lg-8 col-lg-offset-2">
        <%= params[5] %>
      </div>
    </div>

    <% end %>
  </div>
</div>

<%= render partial: 'portraits/partials/recommandations' %>

