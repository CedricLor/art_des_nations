<% content_for :title, t(:portraits, default: "Portraits") %>
<% content_for :body_class, "portraits" %>

<%= render partial: 'portraits/partials/single_views', locals: {
  title: @portrait.title,
  teaser: @portrait.teaser,
  image_url: @portrait.media_containers.first.media,
  body: @portrait.body.html_safe
  }
%>
