<%# aktions/partials/_additional_form_fields.html.erb %>

<% remaining_params.each do |parameter| %>
  <!-- FIXME - Refactor the layout aspects into a layout
  - check portraits/partials/_single_views.html.erb which also needs the same refactoring
  - stay DRY!!!
  -->
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-lg-8 col-lg-offset-2">
      <%= render partial: 'shared/aktion_static/body', locals: {
        body: parameter
        }
      %>
    </div>
  </div>
<% end %>
