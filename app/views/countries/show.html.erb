<%# view: countries/show.html.erb %>

<%= render partial: 'countries/partials/container',
    object: @country,
    locals: {
      parameters: [
        @country.title,
        @country.editorial.html_safe
      ]
    }
%>
